---

- hosts: all

  vars:

    git_config: [
      { key: 'user.name', scope: 'global', value: 'Philip Cutler' },
      { key: 'user.email', scope: 'global', value: 'greenthegarden@gmail.com' },
      { key: 'credential.helper', scope: 'global', value: 'cache' },
    ]

  tasks:

    - name: Configure git
      import_role:
        name: greenthegarden.configure-host
        tasks_from: configure-git.yml
